<!DOCTYPE html>
<html lang="ko">
<head>
    <%- include('./head'); %>
    <link rel="stylesheet" href="/css/index.css">
</head>
<body>
    <div class="container">
        <%- include('./header'); %>
        <main style="min-width: 19rem;">
                <h1>Oraculum</h1>
                <p class="lead">Cover is a one-page template for building simple and beautiful home pages. Download, edit the text, and add your own fullscreen background photo to make it your own.</p>
                <p class="lead">
                    <% if(isLogin) { %>
                        <a href="/main" class="btn btn-lg btn-light fw-bold border-white bg-white">Start</a>
                    <% } else { %>
                        <a href="/account/login" class="btn btn-lg btn-light fw-bold border-white bg-white">Login</a>
                    <% } %>
                    <a href="/admin" class="btn btn-lg btn-light fw-bold border-white bg-white">admin</a>
                </p>
            </div>
        </main>
    </div>
    <footer>
        <%- include('./script'); %>
    </footer>
</body>
</html> 